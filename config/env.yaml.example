# 可选：环境相关配置示例，复制为 env.yaml 后按需修改
chain:
  rpc_url: http://127.0.0.1:8545
  chain_id: 1337

contracts:
  # 合约名 -> 地址（部署后填写）
  # MyToken: "0x..."

wallet_ui:
  base_url: http://localhost:3000
  headless: false

api:
  base_url: http://localhost:8080
  timeout: 30
