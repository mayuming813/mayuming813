# Web3 与链上交互
web3>=6.0.0
eth-account>=0.10.0
eth-abi>=4.0.0

# Solidity 编译（Python 本地编译，不依赖 Remix）
py-solc-x>=1.2.0

# 测试框架
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0

# 钱包 UI 自动化：pynpress 用助记词导入 MetaMask，每次步骤一致
playwright>=1.57.0
pynpress>=0.1.3
# selenium>=4.0.0

# 链上/HTTP API 测试
requests>=2.28.0
httpx>=0.25.0

# 配置与环境
python-dotenv>=1.0.0
pyyaml>=6.0

# 数据校验与工具
jsonschema>=4.0.0
deepdiff>=6.0.0

# 日志与报告（可选）
allure-pytest>=2.13.0
